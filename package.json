{
  "name": "hk-admissions-score-calculator",
  "version": "0.1.0",
  "private": true,
  "description": "Static HKDSE admissions score calculator and programme finder.",
  "scripts": {
    "extract:jupas": "python3 scripts/extract_jupas_scores.py",
    "build:data": "python3 scripts/extract_jupas_scores.py && python3 scripts/build_app_data.py",
    "check": "node --check app/app.js && python3 scripts/extract_jupas_scores.py && python3 scripts/build_app_data.py",
    "audit:data": "python3 scripts/audit_data_quality.py",
    "serve": "python3 -m http.server --bind 127.0.0.1 --directory app 5173"
  }
}
